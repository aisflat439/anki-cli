## Anki CLI

Anki CLI is a terminal client for reviewing anki decks. It's built on [OpenTui](https://github.com/sst/opentui/tree/main/packages/react) using react.

It uses SQLite for storage of cards.
